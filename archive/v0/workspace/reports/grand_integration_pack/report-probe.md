# 隔离 probe 报告 — emberfall_kiln_assize（C-1 头号问题钉死尝试）

真机 MiniMax，~14 拍后被**平台会话额度**截断（非测试问题），FALLBACK=0。产物：`probe.log`、`probe_transcript.md`。**未改引擎/pack。**

## 一句话结论
**头号问题仍未钉死 —— 核心判据（账房娄的零前置物证 `charcoal_ledger_obtained` 能否翻）这次又没测到**：probe 本想消除闭环段的 driver 污染，却撞上**另一种导航污染**——玩家从窑监阁去账房的移动在拍4 触发澄清菜单、driver 没解析选项、**玩家根本没离开窑监阁**，后续拍5–7 的"对娄说"全部发生在玩家其实不在账房的状态下（引擎正确地拒为"娄不在这儿"）。`charcoal_ledger_obtained` 全程 False，但**不是被 arbiter 注入前置锁死，而是请求压根没送达娄**。

## 实际发生
- 拍4「我过账桥前往商会账房」(guild_loft→tally_house，合法连接) → 引擎回「去不了那里」+ 10 选项菜单；driver 未选「10. 商会账房」→ **玩家滞留 guild_loft**（拍5–7 `_在场_` 恒为窑监阔）。
- 拍6 对娄给台阶 → 引擎正确拒「娄不在这儿，TA 在商会账房那边」（玩家确实不在账房）。
- 拍7 要炭账 → 又撞「'勘瓷' 指代不明」菜单（玩家自称"勘瓷"被当歧义实体）。
- → **`charcoal_ledger_obtained` 这条最干净的"零前置物证"判据，两跑（闭环段+本 probe）都因导航/措辞污染，从未真正送达账房娄。结论悬而未决。**

## 唯一有信号的：证人 var（苗）的 partial（但这未必是 bug）
拍7/14 苗处（玩家确实在场）：
- `digger_witness_recorded → partial`，reason＝「苗要先有到场受护、不会事后被追的保障才肯正式落定证词」。
- `digger_testimony_given → partial`，reason＝「苗要先有窑卫放行的硬保障才肯走旧矿道」。
- 引擎 prereq cap **触发了**：`new_prerequisite 'digger_miao_safe_passage_assured' NOT registered (dup/cap)`。
**判读**：这俩恰恰是**惊恐证人合理的角色化条件**（要人身保障才作证），且与我修过的 label「至多需先安抚她、给到场即受护的保障」**一致**——更像 arbiter 行为正确，**不是明显的"不公平注入"**。真正能判"零前置物证被不当注入前置"的，只有账房娄那条，而它没测到。

## 修正：头号问题的当前真实状态
- 闭环段说的"arbiter 给零前置取证 var 注入前置成环" = **尚未坐实**。两次尝试都被**导航/措辞污染**挡在送达之前。
- **真正反复浮现、且确凿的，是「规模下的导航/措辞摩擦」**：多跳或换名目的地的自然语言移动频繁触发菜单、driver 不解析→玩家卡在原地；玩家自称("勘瓷")、句中提及他人都易触发歧义/coherence 拒。**这才是大包里反复绊倒调查动线的确凿障碍**，比 prereq 注入更早、更频繁地让"玩到结局"失败。

## 下一步（留待额度恢复后，必须更严的脚本纪律）
要钉死头号问题，下一个 probe 必须：
1. **每次移动后强制 `/look` 核对 `_在场_` 含目标 NPC 才发请求**；移动失败弹菜单时**解析并选编号**（或直接用精确地点名 `去 商会账房`），到位前不发社交请求。
2. 玩家自称别用"勘瓷"这类会被当实体的词；请求句只含目标 NPC + var 关键词，不夹其他名号。
3. 到账房娄后，祈使+关键词「把官炭私运账交给勘瓷」→ 看 `charcoal_ledger_obtained` 翻 True 还是被注入前置。**这一步干净了，头号问题才有定论。**

## 稳定性 / 包
FALLBACK=0、无崩溃/超时。包 label 加固已生效（lint 清零、load valid），但因没送达娄、加固对 `charcoal_ledger` 的实际效果未验。**未改引擎/pack（仅我此前的 label 编辑）。**
